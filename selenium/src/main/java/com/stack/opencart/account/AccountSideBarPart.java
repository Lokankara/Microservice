package com.stack.opencart.account;

import com.stack.opencart.TopPart;
import org.openqa.selenium.WebDriver;

public class AccountSideBarPart extends TopPart {
    protected AccountSideBarPart(WebDriver driver) {
        super(driver);
    }
}
