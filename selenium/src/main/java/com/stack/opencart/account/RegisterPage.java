package com.stack.opencart.account;

import org.openqa.selenium.WebDriver;

public class RegisterPage extends AccountSideGuestPart {
    protected RegisterPage(WebDriver driver) {
        super(driver);
    }
}
