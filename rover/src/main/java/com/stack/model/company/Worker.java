package com.stack.model.company;

public class Worker extends Employee {

    public Worker(
            int age,
            String name,
            Gender gender,
            double salary) {
        super(age, name, gender, salary);
    }
}
